Go Slices are like flexible arrays.
They can grow or shrink, unlike fixed arrays.
Simple example:
Gonumbers := []int{10, 20, 30}   // slice, no fixed size

Add item: numbers = append(numbers, 40)
Length: len(numbers)
You can take part of a slice easily.